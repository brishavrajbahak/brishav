{
  "name": "brishav-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build && npx wrangler pages dev public --do CONTACT_RATE_LIMITER=ContactRateLimiter@brishav-contact-rate-limiter",
    "build": "node build.js",
    "build:js": "node build.js",
    "lint": "eslint \"public/assets/js/modules/**/*.js\" \"functions/api/v1/**/*.js\" \"functions/lib/**/*.js\" \"workers/**/*.js\" build.js eslint.config.js",
    "preview": "npm run build && npx wrangler pages deploy public --project-name=brishav-portfolio --branch=feature/advanced-v1"
  },
  "devDependencies": {
    "esbuild": "^0.27.0",
    "eslint": "^9.30.1",
    "globals": "^16.3.0"
  }
}
